# Domain docs

This is a single-context repository.

Before exploring, read `CONTEXT.md` when present and relevant ADRs under `docs/adr/`. Missing domain files are not an error; create them lazily only when domain terminology or architectural decisions need to be recorded.

Use the vocabulary defined by `CONTEXT.md` and surface conflicts with existing ADRs explicitly.
